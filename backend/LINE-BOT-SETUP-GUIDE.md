# 📖 คู่มือติดตั้ง LINE Bot แจ้งเตือน

ระบบแจ้งเตือนจุดที่ยังไม่ส่งรายงานและสรุปรายวัน สำหรับระบบเฝ้าระวังไฟป่า

---

## 📋 สารบัญ

1. [สร้าง LINE Official Account](#1-สร้าง-line-official-account)
2. [ตั้งค่า Messaging API](#2-ตั้งค่า-messaging-api)
3. [หา Group ID](#3-หา-group-id)
4. [ติดตั้ง Script ใน Google Apps Script](#4-ติดตั้ง-script-ใน-google-apps-script)
5. [ตั้งค่า Triggers](#5-ตั้งค่า-triggers)
6. [ทดสอบระบบ](#6-ทดสอบระบบ)

---

## 1. สร้าง LINE Official Account

### 1.1 เข้าไปที่ LINE Developers Console

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. ล็อกอินด้วย LINE Account

### 1.2 สร้าง Provider (ถ้ายังไม่มี)

1. คลิก **"Create"** บน Providers
2. ตั้งชื่อ Provider เช่น `อุทยานแห่งชาติเอราวัณ`

### 1.3 สร้าง Channel

1. คลิกที่ Provider ที่สร้าง
2. คลิก **"Create a new channel"**
3. เลือก **"Messaging API"**
4. กรอกข้อมูล:
   - **Channel name**: `ระบบแจ้งเตือนเฝ้าระวังไฟป่า`
   - **Channel description**: `Bot สำหรับแจ้งเตือนการส่งรายงานเฝ้าระวังไฟป่า`
   - **Category**: `Utilities`
   - **Subcategory**: `General`
5. คลิก **"Create"**

---

## 2. ตั้งค่า Messaging API

### 2.1 รับ Channel Access Token

1. ไปที่ Channel ที่สร้าง
2. คลิกแท็บ **"Messaging API"**
3. เลื่อนลงไปที่ **"Channel access token"**
4. คลิก **"Issue"**
5. **คัดลอก Token นี้เก็บไว้** (จะใช้ใน Script)

### 2.2 ปิด Auto-reply

1. ในหน้า Messaging API
2. เลื่อนไปที่ **"LINE Official Account features"**
3. คลิก **"Edit"** ที่ Auto-reply messages
4. **ปิด** Auto-reply messages และ Greeting messages

### 2.3 เพิ่ม Bot เข้ากลุ่ม LINE

1. สแกน QR Code ของ Bot (อยู่ในหน้า Messaging API)
2. Add friend
3. เข้าไปที่กลุ่ม LINE ของจุดเฝ้าระวัง
4. เชิญ Bot เข้ากลุ่ม (ตั้งค่า > เชิญ > เลือก Bot)

---

## 3. หา Group ID

### 3.1 Deploy Webhook ชั่วคราว

1. ใน Google Apps Script ให้สร้างไฟล์ใหม่
2. คัดลอกโค้ดจาก `find-group-id.js`
3. คลิก **Deploy > New deployment**
4. เลือก **Web app**
5. ตั้งค่า:
   - Execute as: **Me**
   - Who has access: **Anyone**
6. คลิก **Deploy** และคัดลอก URL

### 3.2 ตั้งค่า Webhook

1. กลับไปที่ LINE Developers Console
2. ในแท็บ Messaging API เลื่อนไปที่ **"Webhook settings"**
3. คลิก **Edit** ที่ Webhook URL
4. ใส่ URL จาก Step 3.1
5. คลิก **Update**
6. เปิด **"Use webhook"**

### 3.3 ส่งข้อความในกลุ่ม

1. ไปที่กลุ่ม LINE ที่มี Bot อยู่
2. พิมพ์ข้อความอะไรก็ได้
3. กลับมาที่ Apps Script
4. ไปที่ **Executions** (เมนูซ้าย)
5. ดู Log จะเห็น **Group ID** (ขึ้นต้นด้วย `C...`)
6. **คัดลอก Group ID เก็บไว้**

---

## 4. ติดตั้ง Script ใน Google Apps Script

### 4.1 คัดลอก Script

1. ไปที่ [Google Apps Script](https://script.google.com/)
2. เปิด Project ที่ใช้งานอยู่ หรือสร้างใหม่
3. สร้างไฟล์ `line-bot-notification.gs`
4. คัดลอกเนื้อหาจาก `line-bot-notification.js`

### 4.2 ใส่ค่า Config

แก้ไขบรรทัดที่ 9-10:

```javascript
const CHANNEL_ACCESS_TOKEN = 'ใส่_Token_ที่ได้จาก_Step_2.1';
const GROUP_ID = 'ใส่_Group_ID_ที่ได้จาก_Step_3.3';
```

### 4.3 บันทึกและทดสอบ

1. กด **Ctrl+S** เพื่อบันทึก
2. เลือกฟังก์ชัน `testMorningNotification`
3. คลิก **Run**
4. ตรวจสอบข้อความในกลุ่ม LINE

---

## 5. ตั้งค่า Triggers

### 5.1 ใช้ Script อัตโนมัติ

1. คัดลอกเนื้อหาจาก `setup-triggers.js` ไปใส่ต่อท้าย Script
2. เลือกฟังก์ชัน `setupAllTriggers`
3. คลิก **Run**
4. อนุญาต Permissions ที่ถาม

### 5.2 ตรวจสอบ Triggers

1. ไปที่ **Triggers** (ไอคอนนาฬิกาในเมนูซ้าย)
2. ควรเห็น 4 Triggers:
   - `sendMorningNotification` - 10:00
   - `sendAfternoonNotification` - 14:00
   - `sendEveningNotification` - 18:00
   - `sendDailySummary` - 19:00

---

## 6. ทดสอบระบบ

### ทดสอบด้วยมือ

| ฟังก์ชัน | คำอธิบาย |
|----------|----------|
| `testMorningNotification()` | ทดสอบแจ้งเตือนภาคเช้า |
| `sendAfternoonNotification()` | ทดสอบแจ้งเตือนภาคกลางวัน |
| `sendEveningNotification()` | ทดสอบแจ้งเตือนภาคเย็น |
| `testDailySummary()` | ทดสอบสรุปรายวัน |

### ตรวจสอบ Logs

1. ไปที่ **Executions** ในเมนูซ้าย
2. ดูประวัติการทำงานและ Error (ถ้ามี)

---

## ⚠️ การแก้ไขปัญหา

| ปัญหา | วิธีแก้ |
|-------|--------|
| Bot ไม่ส่งข้อความ | ตรวจสอบ Channel Access Token |
| ส่งไม่เข้ากลุ่ม | ตรวจสอบ Group ID และ Bot ต้องอยู่ในกลุ่ม |
| Trigger ไม่ทำงาน | ดู Executions history และตรวจสอบ Error |
| ข้อมูลไม่ถูกต้อง | ตรวจสอบ FOLDER_ID ให้ตรงกับโฟลเดอร์ที่เก็บรูป |

---

## 📞 ติดต่อสอบถาม

หากมีปัญหาในการติดตั้ง โปรดติดต่อผู้ดูแลระบบ
