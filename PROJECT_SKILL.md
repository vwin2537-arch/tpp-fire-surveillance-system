# 🔥 Fire Checkpoint Report System - Project Skill

> **"คัมภีร์ประจำโปรเจค"**
> รวบรวมโครงสร้างระบบ การทำงาน การตั้งค่า และวิธีแก้ปัญหาทั้งหมดไว้ในที่เดียว
> *Created: 2026-01-24*

---

## 1. 🏗️ สถาปัตยกรรมระบบ (System Architecture)

ระบบนี้ทำงานแบบ **Hybrid Web App & LINE Bot** โดยแยกส่วนการทำงานดังนี้:

### 📱 Frontend (Web App)
- **Tech:** React + Vite + Tailwind CSS
- **Host:** (Local/Static Host)
- **หน้าที่:** ให้เจ้าหน้าที่กรอกข้อมูลรายงาน และดู Dashboard
- **จุดสำคัญ:**
  - สร้างไฟล์ชื่อตาม Format: `วันที่/กะ/จุดเฝ้าระวัง`
  - ย่อรูปภาพก่อนส่งเพื่อลดขนาด

### ☁️ Backend (Google Apps Script - GAS)
- **Tech:** Google Apps Script (JavaScript-like)
- **Database:** Google Drive (ใช้ Folder Structure เก็บข้อมูล)
- **หน้าที่:**
  - `doPost`: รับข้อมูลจาก Web App บันทึกลง Drive (และใช้ Echo ID สำหรับ Debug)
  - `doGet`: ส่งข้อมูล JSON กลับไปหน้า Dashboard (มี Cache 10 นาที)
  - `Triggers`: ตั้งเวลาทำงานอัตโนมัติ (10:00, 14:00, 18:00, 19:00)
  - `LINE API`: ยิงแจ้งเตือนผ่าน Messaging API

---

## 2. 📂 โครงสร้างไฟล์สำคัญ (Key Files)

### ฝั่ง Web App (Frontend)
| ไฟล์ | รายละเอียด |
|------|------------|
| `App.tsx` | หัวใจหลักของ Web App รวม Logic หน้า Form และ Dashboard |
| `services/mockDriveService.ts` | **(สำคัญมาก)** ไฟล์รวมฟังก์ชันยิง API ไปหา Backend (`fetch`, `submitReport`) |
| `index.css` | ไฟล์ตกแต่ง Global Styles |

### ฝั่ง Google Apps Script (Backend)
*(อยู่ในโฟลเดอร์ `backend/` ในโปรเจคนี้ เพื่อสำรองข้อมูล)*
| ไฟล์ | รายละเอียด |
|------|------------|
| `line-bot-notification.js` | **Script พระเอก** รวม Logic แจ้งเตือน, Dashboard API, และ LINE Connection |
| `setup-triggers.js` | สคริปต์สำหรับตั้งเวลาทำงานอัตโนมัติ (รันครั้งเดียวจบ) |
| `google-script-schedule.js` | (Legacy) ไฟล์เก่าต้นฉบับ (เก็บไว้ดู Logic เดิม) |

---

## 3. ⚙️ การตั้งค่าที่ต้องรู้ (Configuration)

หากมีการเปลี่ยน Account หรือเปลี่ยนกลุ่ม ต้องแก้ค่าเหล่านี้ใน `line-bot-notification.js`:

```javascript
const CHANNEL_ACCESS_TOKEN = '...'; // Token ยาวๆ จาก LINE Developers
const GROUP_ID = 'C...';            // ID กลุ่มที่ได้จาก Webhook (ไม่ใช่ URL!)
const FOLDER_ID = '...';            // ID โฟลเดอร์ Google Drive ที่เก็บงาน
const TOTAL_WATCH_POINTS = 20;      // จำนวนจุดเฝ้าระวัง
```

---

## 4. 🚀 ขั้นตอนการ Deploy (เมื่อแก้โค้ด)

### กรณีแก้ Backend (Logic การแจ้งเตือน/Dashboard)
1. ก๊อปปี้โค้ดจาก `backend/line-bot-notification.js`
2. ไปที่ **Google Apps Script Editor**
3. วางทับไฟล์เดิม
4. กด **Deploy** > **Manage deployments**
5. กด **Edit (ดินสอ)** > Version: **New version** > กด **Deploy**
   *(ถ้าไม่อัปเดต Version ใหม่ โค้ดเก่าจะยังทำงานอยู่ และ Webhook จะไม่เปลี่ยน)*

### กรณีแก้ Frontend (หน้าเว็บ)
1. แก้โค้ดใน `App.tsx` หรือไฟล์อื่นๆ
2. รันทดสอบ `npm run dev`
3. (ถ้าจะขึ้น Server จริง) รัน `npm run build` แล้วนำไฟล์ใน `dist/` ไปอัปโหลด

---

## 5. 💡 เทคนิคพิเศษที่ใส่ไว้ในระบบ (Secret Sauce)

### A. Dashboard Caching ⚡
หน้า Dashboard จะไม่ดึงข้อมูลสดจาก Drive ทุกครั้ง (เพราะช้ามาก)
- **Logic:** `CacheService.getScriptCache()`
- **Duration:** เก็บข้อมูลไว้ **10 นาที** (600 วินาที)
- **ผลลัพธ์:** โหลดเร็วปรื๋อในครั้งที่ 2 เป็นต้นไป

### B. Date Timezone Fix 🕒
JavaScript ปกติจะใช้ UTC เวลาแปลงเป็น String ซึ่งจะทำให้เวลาเช้าตรู่ของไทย (UTC+7) กลายเป็น "เมื่อวาน"
- **Solution:** สร้างฟังก์ชัน `getToday()` เอง โดยดึง `getFullYear`, `getMonth`, `getDate` มาต่อ String เองตรงๆ
- **ผลลัพธ์:** วันที่ในแอพตรงกับวันจริงเสมอ ไม่มีดีเลย์

### C. Race Condition Guard 🛡️
ป้องกันข้อมูลหน้า Dashboard ตีกันเมื่อกดเปลี่ยนวันรัวๆ
- **Solution:** ใช้ `useRef` เก็บ `activeRequestDate` เช็คว่าข้อมูลที่โหลดเสร็จมาช้ากว่าการเปลี่ยนหน้าล่าสุดไหม ถ้าใช่ให้ทิ้งไป

---

## 6. 🛠️ การแก้ปัญหาที่พบบ่อย (Troubleshooting)

**Q: บอทไม่แจ้งเตือน?**
- A1: เช็คว่า `setupAllTriggers()` ถูกรันหรือยัง?
- A2: เช็คว่าบอทถูกเตะออกจากกลุ่มไหม?
- A3: เช็ค Quota ข้อความฟรี (200 ข้อความ/เดือน)

**Q: Dashboard ข้อมูลไม่อัปเดต?**
- A: รอ 10 นาที (ติด Cache) หรือแก้โค้ด Backend ให้ Cache สั้นลงถ้าต้องการ Realtime จริงๆ

**Q: หา Group ID ไม่เจอ/ไม่มั่นใจ?**
- A: เปิด Webhook ใน LINE Dev -> ตั้งค่า URL -> พิมพ์แชทในกลุ่ม -> บอทจะตอบ ID กลับมา (ด้วยฟังก์ชัน `doPost`)

---
*บันทึกไว้ให้นายช่างใหญ่ และ AI รุ่นต่อไปดูแลต่อได้ง่ายๆ ครับ!* 🤖🔧
